[package]
name = "const_shader_aligned"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = {
  version = "1.25.1",
  default-features = false,
  features = ["derive"]
}
glam = {
  version = "0.33.2",
  default-features = false,
  features = ["bytemuck", "i16", "i32", "std", "u16", "u32"]
}
half = { version = "2.7.1", default-features = false, features = ["bytemuck"] }
